// Package descriptors is one of the core Iterum packages, defining the different fragment structures.
// It defines `LocalFragmentDesc, LocalFileDesc, RemoteFragmentDesc, RemoteFileDesc, FragmentID and KillMessage`.
// these structures describe data by referencing it without actually having it in memory and are the core message types
// that are passed around within Iterum.
package descriptors
