// Package socket contains two structures for communicating between sidecar and user-defined containers.
// It uses the `transmit` package for the transmission protocol to send and receive data, but offers
// a simple interface with Go channels for connecting them to your application
package socket
